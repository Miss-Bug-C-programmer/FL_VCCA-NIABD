[pytest]
markers =
    fast: lightweight tests that do not download data, run full training, require GPU, or depend on long network simulation
    integration: minimal end-to-end checks across modules
    cpu_integration: bounded CPU end-to-end publication smoke tests
    slow: full training, related-baseline smoke, or longer network simulation checks
    gpu_smoke: explicit GPU smoke commands for publication validation, excluded from default CPU CI
